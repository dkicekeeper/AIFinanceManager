# 🎯 Система интеллектуального ранжирования счетов

## Обзор

Система автоматически ранжирует счета по частоте использования с адаптивным автоподставлением на основе контекста транзакции и истории использования.

## Архитектура

```
AccountRankingService (Core Service)
         ↓
AccountsViewModel (Integration Layer)
         ↓
Views (UI Components)
  ├── QuickAddTransactionView
  ├── AddTransactionModal
  └── AccountSelectorView
```

## Компоненты

### 1. `AccountRankingService.swift`

Основной сервис для ранжирования счетов.

**Ключевые методы:**
- `rankAccounts()` - Ранжирует счета с учетом контекста
- `suggestedAccount()` - Возвращает рекомендуемый счет для категории (адаптивное автоподставление)

### 2. `AccountsViewModel` (расширение)

Интеграция сервиса в ViewModel.

**Новые методы:**
```swift
// Получить ранжированные счета
func rankedAccounts(
    transactions: [Transaction],
    type: TransactionType,
    amount: Double? = nil,
    category: String? = nil,
    sourceAccountId: String? = nil
) -> [Account]

// Получить рекомендуемый счет для категории
func suggestedAccount(
    forCategory category: String,
    transactions: [Transaction],
    amount: Double? = nil
) -> Account?
```

## Алгоритм ранжирования

### Формула базового score:

```
Score = (last30Days × 3.0) + (last90Days × 2.0) + (allTime × 1.0)
```

### Бонусы и штрафы:

| Фактор | Модификатор | Условие |
|--------|-------------|---------|
| 🎯 **Категория совпадает** | +25 баллов | Счет часто используется для данной категории |
| 📅 **Недавно использован** | +10 баллов | Использован в последние 7 дней |
| 💰 **Достаточный баланс** | +5 баллов | Balance ≥ amount (для расходов) |
| 🆕 **Новый счет** | +20 баллов | Создан менее 7 дней назад (временный бонус) |
| 🏦 **Депозит** | -50 баллов | Менее удобен для частых операций |
| 💤 **Неактивен** | -100 баллов | Не использовался >180 дней И balance = 0 |
| 🚫 **Исключить** | -1000 баллов | Source account при переводе |

### Примеры работы:

#### Кейс 1: Простое ранжирование
```
Счет A: 5 транзакций за 30 дней → Score = 5×3 = 15 баллов
Счет B: 2 транзакции за 30 дней → Score = 2×3 = 6 баллов
Результат: A > B
```

#### Кейс 2: Адаптивное подставление по категории
```
Категория: "Продукты"
История:
  - Счет "Kaspi Gold": 10 транзакций по категории "Продукты"
  - Счет "Halyk": 2 транзакции по категории "Продукты"

Результат: При создании расхода по категории "Продукты" 
автоматически подставляется "Kaspi Gold"
```

#### Кейс 3: Учет баланса
```
Создание расхода на 5000₸:
  - Счет A: 10000₸ баланс → Score + 5 (достаточно)
  - Счет B: 1000₸ баланс → Score + 0 (недостаточно)
Результат: A получает бонус
```

#### Кейс 4: Новые пользователи (без истории)
```
Smart Defaults:
1. Обычные счета > депозиты
2. Счета с балансом > amount (для расходов)
3. Больший баланс > меньший баланс
4. Новее > старше
5. По алфавиту
```

## Адаптивное автоподставление

### Как работает:

1. **Анализ истории**: Система анализирует все транзакции для данной категории
2. **Подсчет частоты**: Подсчитывается, какой счет чаще всего используется
3. **Проверка актуальности**: Учитывается дата последнего использования
4. **Проверка баланса**: Если указана сумма, проверяется достаточность баланса
5. **Автоподставление**: Счет автоматически выбирается при открытии модального окна

### Пример реального использования:

```swift
// Пользователь нажимает на категорию "Продукты"
// Система автоматически:
1. Анализирует 50 последних транзакций по категории "Продукты"
2. Выясняет, что 40 из них были со счета "Kaspi Gold"
3. Проверяет, что "Kaspi Gold" использовался недавно
4. Автоматически подставляет "Kaspi Gold" в форму

// Пользователь вводит сумму 10000₸
// Система пересчитывает:
1. "Kaspi Gold" баланс: 5000₸ (недостаточно)
2. Переключается на следующий подходящий счет с балансом ≥ 10000₸
```

## Интеграция в UI

### AddTransactionModal

```swift
// 1. Адаптивное автоподставление при открытии
private func setupOnAppear() {
    if selectedAccountId == nil {
        if let suggested = accountsViewModel.suggestedAccount(
            forCategory: category,
            transactions: transactionsViewModel.allTransactions
        ) {
            selectedAccountId = suggested.id
        }
    }
}

// 2. Ранжированный список счетов
private var rankedAccounts: [Account] {
    accountsViewModel.rankedAccounts(
        transactions: transactionsViewModel.allTransactions,
        type: type,
        amount: parsedAmount,
        category: category
    )
}
```

## Преимущества

### Для пользователя:
✅ **Экономия времени** - счет подставляется автоматически  
✅ **Умное предложение** - учитывается контекст и история  
✅ **Меньше ошибок** - система предлагает счета с достаточным балансом  
✅ **Персонализация** - адаптируется под привычки пользователя  

### Для разработчика:
✅ **Переиспользуемый сервис** - единая логика ранжирования  
✅ **Расширяемость** - легко добавить новые факторы ранжирования  
✅ **Тестируемость** - изолированный сервис легко тестировать  
✅ **Гибкость** - контекстное ранжирование для разных сценариев  

## Будущие улучшения

### Фаза 2 (опционально):

1. **Временные паттерны**
   - Утром чаще используется счет A, вечером - B
   - Учитывать день недели (выходные vs будни)

2. **Машинное обучение**
   - Предсказание счета на основе суммы + категория + время
   - Обучение на истории пользователя

3. **Пользовательские настройки**
   - Закрепление счетов наверху
   - Скрытие неиспользуемых счетов
   - Переопределение приоритетов

4. **Контекстные подсказки**
   - "Вы обычно используете Kaspi для продуктов"
   - "У счета A недостаточно средств"

## Логирование

Для отладки добавлено логирование:

```swift
print("🎯 [ADAPTIVE] Suggested account '\(account.name)' for category '\(category)'")
```

Префиксы логов:
- `🎯 [ADAPTIVE]` - адаптивное автоподставление
- `📊 [RANKING]` - процесс ранжирования
- `💰 [BALANCE]` - проверка баланса

## Производительность

- ⚡ **O(n)** сложность для ранжирования n счетов
- 📦 **Кеширование** - computed property пересчитывается только при изменении данных
- 🚀 **Ленивые вычисления** - score рассчитывается только при необходимости

## Тестирование

Рекомендуемые тест-кейсы:

```swift
// 1. Базовое ранжирование
func testBasicRanking() {
    // Счет с большим количеством транзакций должен быть первым
}

// 2. Адаптивное подставление
func testCategoryBasedSuggestion() {
    // Счет, часто используемый для категории, должен предлагаться
}

// 3. Проверка баланса
func testSufficientBalance() {
    // Счета с достаточным балансом должны получать бонус
}

// 4. Smart defaults
func testNewUserExperience() {
    // Без истории должен использоваться smart defaults
}
```

## Заключение

Система интеллектуального ранжирования счетов значительно улучшает UX приложения, делая его более персонализированным и удобным. Адаптивное автоподставление экономит время пользователя и минимизирует количество ошибок при вводе транзакций.
