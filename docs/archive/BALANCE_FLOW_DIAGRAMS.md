# 📊 Диаграммы flow балансов

**Дата:** 2026-02-03
**Цель:** Визуализация проблем и решений для balance operations

---

## 1. ❌ CURRENT (BROKEN): Internal Transfer Flow

```
┌──────────────────────────────────────────────────────────────────┐
│ USER ACTION: Transfer 100 KZT from Account A to Account B       │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ TransactionsViewModel.transfer()                                 │
│   • sourceId: "A"                                                │
│   • targetId: "B"                                                │
│   • amount: 100                                                  │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ AccountOperationService.transfer()                               │
│   ❌ PROBLEM: Bypasses BalanceCoordinator!                      │
└──────────────────────────────────────────────────────────────────┘
                    ↙                     ↘
        ┌───────────────┐      ┌────────────────────┐
        │ deduct(A, 100)│      │ add(B, 100)        │
        │ ❌ Direct mod │      │ ❌ Direct mod      │
        └───────────────┘      └────────────────────┘
                    ↓                       ↓
        ┌───────────────┐      ┌────────────────────┐
        │ A.balance     │      │ B.balance          │
        │ -= 100        │      │ += 100             │
        │ ❌ WRONG!     │      │ ❌ WRONG!          │
        └───────────────┘      └────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ accountBalanceService.syncAccountBalances()                      │
│   ❌ Updates Account.initialBalance instead of BalanceCoordinator│
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ Create Transaction                                               │
│   • id: "tx_123"                                                 │
│   • amount: 100                                                  │
│   • accountId: "A"                                               │
│   • targetAccountId: "B"                                         │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ allTransactions.append(transferTx)                               │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ BalanceCoordinator.updateForTransaction()                       │
│   ⚠️ Called AFTER balance already modified                      │
└──────────────────────────────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────────┐
        │ processAddTransaction(transferTx)          │
        └────────────────────────────────────────────┘
                    ↙                     ↘
   ┌────────────────────┐      ┌─────────────────────────────┐
   │ Process Source "A" │      │ Process Target "B"          │
   │ ✅ isSource=true   │      │ ❌ isSource NOT SPECIFIED  │
   │ (default)          │      │    (defaults to true)       │
   └────────────────────┘      └─────────────────────────────┘
            ↓                              ↓
   ┌────────────────────┐      ┌─────────────────────────────┐
   │ applyTransaction() │      │ applyTransaction()          │
   │ balance -= 100     │      │ ❌ balance -= 100 (WRONG!) │
   │ ✅ CORRECT         │      │    (should be += 100)       │
   └────────────────────┘      └─────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────────┐
        │ RESULT:                                    │
        │ A: 1000 → 900 ✅ CORRECT                   │
        │ B: 500 → 400 ❌ WRONG (should be 600)     │
        └────────────────────────────────────────────┘
```

**ПРОБЛЕМЫ:**
1. `AccountOperationService` модифицирует балансы напрямую (нарушение Single Source of Truth)
2. `BalanceCoordinator.processAddTransaction()` вызывается с `isSource` не указанным для target
3. Баланс target account вычитается вместо добавления

---

## 2. ✅ FIXED: Internal Transfer Flow

```
┌──────────────────────────────────────────────────────────────────┐
│ USER ACTION: Transfer 100 KZT from Account A to Account B       │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ TransactionsViewModel.transfer()                                 │
│   • sourceId: "A"                                                │
│   • targetId: "B"                                                │
│   • amount: 100                                                  │
│   ✅ passes balanceCoordinator                                  │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ AccountOperationService.transfer()                               │
│   ✅ NO direct balance modification                             │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ Create Transaction FIRST                                         │
│   • id: "tx_123"                                                 │
│   • amount: 100                                                  │
│   • accountId: "A"                                               │
│   • targetAccountId: "B"                                         │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ allTransactions.append(transferTx)                               │
└──────────────────────────────────────────────────────────────────┘
                              ↓
┌──────────────────────────────────────────────────────────────────┐
│ ✅ BalanceCoordinator.updateForTransaction()                    │
│    (Single entry point for all balance updates)                 │
└──────────────────────────────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────────┐
        │ processAddTransaction(transferTx)          │
        └────────────────────────────────────────────┘
                    ↙                     ↘
   ┌────────────────────┐      ┌─────────────────────────────┐
   │ Process Source "A" │      │ Process Target "B"          │
   │ ✅ isSource=true   │      │ ✅ isSource=false (FIXED!)  │
   │                    │      │                             │
   └────────────────────┘      └─────────────────────────────┘
            ↓                              ↓
   ┌────────────────────┐      ┌─────────────────────────────┐
   │ applyTransaction() │      │ applyTransaction()          │
   │ balance -= 100     │      │ ✅ balance += 100 (CORRECT)│
   │ ✅ CORRECT         │      │                             │
   └────────────────────┘      └─────────────────────────────┘
                    ↓                       ↓
   ┌────────────────────┐      ┌─────────────────────────────┐
   │ BalanceStore       │      │ BalanceStore                │
   │ setBalance(900, A) │      │ setBalance(600, B)          │
   └────────────────────┘      └─────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────────┐
        │ balances = updatedBalances                 │
        │ ✅ Single @Published update triggers UI   │
        └────────────────────────────────────────────┘
                              ↓
        ┌────────────────────────────────────────────┐
        │ RESULT:                                    │
        │ A: 1000 → 900 ✅ CORRECT                   │
        │ B: 500 → 600 ✅ CORRECT                    │
        └────────────────────────────────────────────┘
```

**РЕШЕНИЯ:**
1. ✅ `AccountOperationService` НЕ модифицирует балансы напрямую
2. ✅ Создается транзакция ПЕРЕД обновлением балансов
3. ✅ `BalanceCoordinator` получает транзакцию и обрабатывает оба аккаунта правильно
4. ✅ `isSource=false` передается для target account
5. ✅ Единственная публикация в конце → UI обновляется атомарно

---

## 3. Single Source of Truth Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SINGLE SOURCE OF TRUTH                           │
│                                                                     │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ Account.initialBalance (Domain Model)                      │   │
│  │   • Stored for manual accounts                             │   │
│  │   • nil for shouldCalculateFromTransactions=true           │   │
│  └────────────────────────────────────────────────────────────┘   │
│                            ↓ Persisted                             │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ AccountEntity.balance (CoreData)                           │   │
│  │   • Persistent storage                                     │   │
│  │   • Synced on saveToStorage()                              │   │
│  └────────────────────────────────────────────────────────────┘   │
│                            ↓ Loaded                                │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ BalanceStore (Runtime State)                               │   │
│  │   • accounts: [accountId: AccountBalance]                  │   │
│  │   • calculationModes: [accountId: BalanceMode]             │   │
│  │   • @Published balances: [accountId: Double]               │   │
│  └────────────────────────────────────────────────────────────┘   │
│                            ↓ Computed                              │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ BalanceCalculationEngine                                   │   │
│  │   • calculateBalance() - O(n) full calculation             │   │
│  │   • applyTransaction() - O(1) incremental                  │   │
│  └────────────────────────────────────────────────────────────┘   │
│                            ↓ Orchestrated by                       │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ BalanceCoordinator (Facade)                                │   │
│  │   • updateForTransaction() - single entry point            │   │
│  │   • recalculateAll() - batch updates                       │   │
│  └────────────────────────────────────────────────────────────┘   │
│                            ↓ Observed by                           │
│  ┌────────────────────────────────────────────────────────────┐   │
│  │ UI Views (SwiftUI)                                         │   │
│  │   • AccountCard                                            │   │
│  │   • TransactionCard                                        │   │
│  │   • HistoryView                                            │   │
│  └────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 4. Transaction Lifecycle

```
┌──────────────────────────────────────────────────────────────────┐
│ 1. CREATE TRANSACTION                                            │
└──────────────────────────────────────────────────────────────────┘
   │
   │  let tx = Transaction(...)
   │  allTransactions.append(tx)
   │
   ↓
┌──────────────────────────────────────────────────────────────────┐
│ 2. UPDATE BALANCES (via BalanceCoordinator)                     │
└──────────────────────────────────────────────────────────────────┘
   │
   │  await coordinator.updateForTransaction(tx, operation: .add(tx))
   │
   ↓
┌──────────────────────────────────────────────────────────────────┐
│ 3. BALANCE COORDINATOR PROCESSING                                │
└──────────────────────────────────────────────────────────────────┘
   │
   ├─→ Income/Expense: 1 account affected
   │   └─→ applyTransaction(isSource: true)
   │
   ├─→ Internal Transfer: 2 accounts affected
   │   ├─→ Source: applyTransaction(isSource: true)
   │   └─→ Target: applyTransaction(isSource: false) ⚠️ CRITICAL
   │
   └─→ Deposit operations: 1 account + depositInfo
       └─→ applyTransactionToDeposit()
   │
   ↓
┌──────────────────────────────────────────────────────────────────┐
│ 4. BALANCE STORE UPDATE                                          │
└──────────────────────────────────────────────────────────────────┘
   │
   │  store.setBalance(newBalance, for: accountId)
   │  balances[accountId] = newBalance
   │
   ↓
┌──────────────────────────────────────────────────────────────────┐
│ 5. UI UPDATE (via @Published)                                   │
└──────────────────────────────────────────────────────────────────┘
   │
   │  self.balances = updatedBalances  // Triggers SwiftUI update
   │
   ↓
┌──────────────────────────────────────────────────────────────────┐
│ 6. PERSIST TO STORAGE                                            │
└──────────────────────────────────────────────────────────────────┘
   │
   │  saveToStorage() → CoreData
   │
   ✅ COMPLETE
```

---

## 5. Balance Calculation Modes

```
┌─────────────────────────────────────────────────────────────────┐
│ MANUAL ACCOUNT (shouldCalculateFromTransactions = false)       │
└─────────────────────────────────────────────────────────────────┘

  Initial State:
    initialBalance: 1000
    transactions: []

  Balance Calculation:
    balance = initialBalance + Σ(transactions)
    balance = 1000 + 0 = 1000

  After adding Income(100):
    balance = initialBalance + Σ(transactions)
    balance = 1000 + 100 = 1100
          └─────┬─────┘   └───┬───┘
            Fixed        Computed


┌─────────────────────────────────────────────────────────────────┐
│ IMPORTED ACCOUNT (shouldCalculateFromTransactions = true)      │
└─────────────────────────────────────────────────────────────────┘

  Initial State:
    initialBalance: nil (or 0)
    transactions: [imported from CSV]

  Balance Calculation:
    Step 1: Calculate initialBalance
      initialBalance = current - Σ(transactions)
      initialBalance = 0 - (-1000) = 1000
                          └────┬────┘
                        Sum of all imported txs

    Step 2: Store calculated initialBalance
      account.initialBalance = 1000

    Step 3: Mark as "preserveImported"
      BalanceStore.markAsImported(accountId)

  After adding NEW Income(100):
    balance = initialBalance + Σ(NEW transactions only)
    balance = 1000 + 100 = 1100
          └───┬────┘   └─┬─┘
          Calculated   New tx


┌─────────────────────────────────────────────────────────────────┐
│ KEY DIFFERENCES                                                 │
└─────────────────────────────────────────────────────────────────┘

  Manual Account:
    • User provides initialBalance
    • All transactions applied incrementally
    • Mode: fromInitialBalance

  Imported Account:
    • initialBalance calculated from CSV data
    • Only NEW transactions applied
    • Mode: preserveImported
    • CRITICAL: Don't double-count imported transactions!
```

---

## 6. BalanceCalculationEngine - isSource Logic

```
┌─────────────────────────────────────────────────────────────────┐
│ applyTransaction(tx, currentBalance, account, isSource)         │
└─────────────────────────────────────────────────────────────────┘

INCOME:
  isSource: irrelevant (only 1 account involved)
  formula: balance += amount
  example: 1000 + 100 = 1100

EXPENSE:
  isSource: irrelevant (only 1 account involved)
  formula: balance -= amount
  example: 1000 - 100 = 900

INTERNAL TRANSFER:
  isSource: CRITICAL! Determines direction

  SOURCE ACCOUNT (isSource=true):
    formula: balance -= amount
    example: 1000 - 100 = 900
    ✅ Money leaves account

  TARGET ACCOUNT (isSource=false):
    formula: balance += amount
    example: 500 + 100 = 600
    ✅ Money enters account

  ❌ WRONG (isSource not specified, defaults to true):
    TARGET treated as SOURCE
    formula: balance -= amount
    example: 500 - 100 = 400
    ❌ Money incorrectly leaves instead of entering!


┌─────────────────────────────────────────────────────────────────┐
│ VISUAL: Transfer 100 from A to B                               │
└─────────────────────────────────────────────────────────────────┘

  ✅ CORRECT:
    A (isSource=true):  1000 - 100 = 900  ✓
    B (isSource=false): 500 + 100 = 600   ✓

  ❌ WRONG (isSource not specified):
    A (isSource=true):  1000 - 100 = 900  ✓
    B (isSource=true):  500 - 100 = 400   ✗  <-- BUG!
                            ^^^
                        Should be +
```

---

## 7. Performance: Full Recalculation vs Incremental

```
┌─────────────────────────────────────────────────────────────────┐
│ FULL RECALCULATION (O(n×m))                                     │
└─────────────────────────────────────────────────────────────────┘

  Used for: CSV Import, Reset, Full Sync

  Process:
    for each account in accounts:  // n iterations
      initialBalance = ...
      for each transaction in transactions:  // m iterations
        if transaction affects this account:
          apply to balance
      finalBalance = initialBalance + sum

  Complexity: O(n×m) where n=accounts, m=transactions
  Example: 10 accounts × 1000 transactions = 10,000 iterations


┌─────────────────────────────────────────────────────────────────┐
│ INCREMENTAL UPDATE (O(1))                                       │
└─────────────────────────────────────────────────────────────────┘

  Used for: Add/Update/Delete single transaction

  Process:
    currentBalance = store.getBalance(accountId)  // O(1)
    delta = calculateDelta(transaction)           // O(1)
    newBalance = currentBalance + delta           // O(1)
    store.setBalance(newBalance, accountId)       // O(1)

  Complexity: O(1) - constant time
  Example: 1 lookup + 1 calculation = 2 operations


┌─────────────────────────────────────────────────────────────────┐
│ PERFORMANCE COMPARISON                                          │
└─────────────────────────────────────────────────────────────────┘

  Operation: Add Income Transaction
    Full Recalc: ~500ms (10 accounts × 1000 txs)
    Incremental: <1ms (direct calculation)
    Speedup: 500x faster ⚡

  Operation: CSV Import (1000 transactions)
    Full Recalc: ~2s (necessary for initial state)
    Incremental: N/A (requires initial state first)

  Operation: Transfer (affects 2 accounts)
    Full Recalc: ~500ms
    Incremental: <2ms
    Speedup: 250x faster ⚡
```

---

## 8. Error Handling Flow

```
┌──────────────────────────────────────────────────────────────────┐
│ Happy Path: Transfer Success                                    │
└──────────────────────────────────────────────────────────────────┘
   Create Transaction
        ↓
   Update BalanceCoordinator
        ↓
   ✅ Success
        ↓
   Publish UI Update
        ↓
   Save to Storage


┌──────────────────────────────────────────────────────────────────┐
│ Error Path: BalanceCoordinator Not Initialized                  │
└──────────────────────────────────────────────────────────────────┘
   Create Transaction
        ↓
   Check coordinator != nil
        ↓
   ❌ nil → Log warning
        ↓
   ⚠️ Balances NOT updated
        ↓
   Transaction still saved
        ↓
   User sees stale balance
        ↓
   MITIGATION: recalculateAll() on next launch


┌──────────────────────────────────────────────────────────────────┐
│ Error Path: Account Not Found                                   │
└──────────────────────────────────────────────────────────────────┘
   Update BalanceCoordinator
        ↓
   store.getAccount(accountId)
        ↓
   ❌ nil → Silent failure (guard return)
        ↓
   #if DEBUG: assertionFailure()
        ↓
   Balance NOT updated
        ↓
   MITIGATION: Add error logging


┌──────────────────────────────────────────────────────────────────┐
│ Recovery: Optimistic Update + Revert                            │
└──────────────────────────────────────────────────────────────────┘
   optimisticUpdate(accountId, delta)
        ↓
   Show new balance immediately
        ↓
   Attempt network operation
        ↓
   ❌ Network error
        ↓
   revertOptimisticUpdate(operationId)
        ↓
   Restore previous balance
        ↓
   Show error message
```

---

**Автор:** Claude Code Agent
**Дата:** 2026-02-03
**Версия:** 1.0
**Статус:** ✅ Diagrams Complete
